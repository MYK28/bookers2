  <h2>User info</h2>
  <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format:'jpeg', fallback: "no_image.jpg" %> <!-- @userをuserに変更-->
  <table class="table">
    <tbody>
      <tr></tr>
      <tr>
        <th>name</th>
        <th><%= user.name %></th> <!-- @userをuserに変更-->
      </tr>
      <tr>
        <th>introduction</th>
        <th><%= user.introduction %></th> <!-- @userをuserに変更-->
      </tr>
    </tbody>
  </table>
  <%= link_to edit_user_path(user), class: "btn btn-outline-secondary btn-block" do %> <!-- @userをuserに変更-->
    <i class="fas fa-user-cog"></i>
  <% end %>